const susupportedLngs = ['en', 'es'];
const i18nNamespaces = ['pages', 'alert'];

// We use module.exports here so we can also import them in the i18next-parser.config.js file
module.exports = {
  susupportedLngs,
  i18nNamespaces
};
